### ERR-006: 固件广播名竞态——真机空中广播名为组件硬编码 "nimble-ble-ota" 而非 "C6-OTA-1128"

- **症状**: 上位机 BleakScanner 实测（Windows 真实蓝牙环境）：设备 14:C1:9F:E5:11:2A 空中广播名为 `nimble-ble-ota`（组件 host_init 里设置的默认名），不是 `C6-OTA-1128`。TASK-002 验收时的 "adv C6-OTA-1128" 锚点日志只证明 GAP 名已设置（`ble_svc_gap_device_name()` 返回值），不等于空中广播数据用了这个名字
- **根因**: ble_ota 组件 `esp_ble_ota_on_sync()` 回调（nimble_ota.c:1112-1136，host 协议栈同步完成时触发，异步于 app_main）内部调用 `esp_ble_ota_advertise()`，其中**实时读取** `ble_svc_gap_device_name()` 构造广播 fields。firmware/main/main.c:134-138 的顺序是 `esp_ble_ota_host_init()`（内部启动 NimBLE host task）→ 立即 `ble_svc_gap_device_name_set("C6-OTA-1128")`，注释假设 host 同步"需完成多轮 HCI 命令（毫秒级）必然慢于 name_set"。本机实测该假设不成立：host 同步（可能因缓存/时序）先于 name_set 完成广播构造，广播数据定格为旧名 "nimble-ble-ota"。BLE 广播名在 adv fields 构造那一刻固化，之后改 GAP 名不影响已启动的广播
- **修复**: 固件侧（待办，不在本任务范围）：name_set 必须挪到 host_init **之前**（NimBLE GAP 服务初始化前设置名字是否生效需验证；稳妥方案是自定义 adv：等待 ble_gap_adv_active 后停广播→改名→重启广播；或用 ble_hs_sync_callback 先注册同步回调，同步后再 set name 并手动起广播）。上位机侧已做兼容：find_device 支持 --mac 按 MAC 匹配、scan 同时列出 nimble-ble-ota 别名
- **规律提炼**: "先 init 后配置"的时序假设必须以**空中/输出侧的最终消费时机**为准验证，不能以函数返回顺序推断。异步框架里 init 函数返回 ≠ init 的回调链执行完毕；凡"回调里读取配置"的模式，配置必须先于 init 或等待同步点后再改。日志锚点打印的是"软件视角状态"，与"外部视角真实表现"（空口抓包/对端所见）是两回事，验收必须看后者
- **二次提炼接口**: META 规则 —— [ASYNC] 组件/库在异步回调中读取全局配置时，app 侧配置写入必须先于 init 调用，或挂接到同步点之后；禁止用"我的写操作在函数返回前"推断竞态胜出。验收外设行为以对端/空口实测为准
- **首次出现**: 2026-09-01
- **复现次数**: 1（首现即空中实测捕获）

## 修复记录（2026-09-02，embedded-firmware-engineer）

- **方案**: 方案 A——本地 vendor 组件。将 `managed_components/espressif__ble_ota`（v0.1.17）整体拷入 `firmware/components/ble_ota`，patch `nimble_ota.c` 的 name_set：`ble_svc_gap_device_name_set("nimble-ble-ota")` → `ble_svc_gap_device_name_set(CONFIG_BLE_OTA_DEVICE_NAME)`，组件 Kconfig 新增 `BLE_OTA_DEVICE_NAME`（string，默认 "nimble-ble-ota"），`sdkconfig.defaults` 设 `CONFIG_BLE_OTA_DEVICE_NAME="C6-OTA-1128"`；补 `#include "sdkconfig.h"`。`main/idf_component.yml` 移除 `espressif/ble_ota`（vendor 组件自带 idf_component.yml 继续声明 cjson/esp_encrypted_img/cmake_utilities 依赖）；`main/CMakeLists.txt` 显式 REQUIRES ble_ota（本地组件不自动注入 main 依赖）。app_main 删除 host_init 后的 name_set 覆盖（第 8 步），广播名完全由组件在 host task 启动前写入
- **选型理由**: 构建环境实测（wslbuild.sh 的 rsync 每次 --delete 会清掉 WSL 侧 managed_components/ 与 sdkconfig 再重新下载/生成）证明方案 B（patch managed_components + rsync exclude）的 patch 完全游离于版本控制且依赖脚本细节；vendor 方案 patch 进 git、Kconfig 定制点语义清晰、fullclean/换机不丢
- **竞态消除原理**: 组件原有的 name_set 位置（host_init 内、`nimble_port_freertos_init` 之前）本身是竞态安全的——host task 尚未启动，sync_cb 不可能先读。仅把写入值从硬编码换成 Kconfig 配置值，零新增时序风险
- **验证**:
  - 构建零错误（bin 0xa0550=656,720B，app 分区余 64%），烧录成功 hash verified
  - 依赖解析证据：构建后 `managed_components/` 仅剩 cjson/cmake_utilities/esp_encrypted_img/esp_secure_cert_mgr，无 `espressif__ble_ota`；dependencies.lock 的 direct_dependencies 更新为 cjson+cmake_utilities（经 vendor 组件 manifest 传递）
  - 代码级确认：`strings build/esp32c6_ble_ota.bin` 仅含 "C6-OTA-1128"，"nimble-ble-ota" 已不在二进制中；sdkconfig 含 `CONFIG_BLE_OTA_DEVICE_NAME="C6-OTA-1128"`
  - 空口广播名实测由 PM 走 Windows 扫描确认（烧录后需手动 RST，见 ERR-003）
- **遗留**: `ota_shared/include/ota_version.h` 的 `OTA_BLE_DEVICE_NAME` 宏当前无代码引用（shared 层常量保留，未动业务层）
