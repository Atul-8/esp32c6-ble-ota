# 任务表（PM 维护）

> 开发者通过 `/ai:pm` 提需求 → PM 归类写入此表 → 专家按分配执行，互不干扰。
> PM 用 `tasks.js` 维护；专家读取自己负责的任务（@expert）。

## 待办（pending）
- [ ] TASK-001 [BUILD] 创建git仓库+整理README+建立issue推动计划+同步创建gitee仓库（自建自推） @pm

## 进行中（doing）

## 完成（done）
